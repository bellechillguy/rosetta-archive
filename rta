#!/usr/bin/env python3
"""Repository-local launcher that needs no installation step."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parent / "src"))

from rosetta_archive.cli import main

raise SystemExit(main())

